# =============================================================================
# CC-Proxy Backend Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (NeonDB / PostgreSQL)
# -----------------------------------------------------------------------------
# Format: postgresql://user:password@host:port/database?sslmode=require
# For local testing with docker-compose: postgresql://ccproxy:dev_password_change_in_production@localhost:5432/ccproxy
DATABASE_URL=postgresql://user:password@hostname/database

# -----------------------------------------------------------------------------
# Google OAuth Configuration
# -----------------------------------------------------------------------------
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/auth/google/callback
# 3. Copy the client ID and secret here
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret

# OAuth Redirect URI - must match what you configured in Google Console
# Change this to your production URL when deploying
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# -----------------------------------------------------------------------------
# Session Security
# -----------------------------------------------------------------------------
# Random secret key for signing session cookies (minimum 64 bytes)
# Generate with: openssl rand -base64 64
SESSION_SECRET=change_me_to_a_random_secret_minimum_64_bytes_long_for_cookie_signing

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=3000

# Base URL of the server (used for device flow authentication)
# Defaults to http://localhost:PORT if not set
# Set this to your production URL when deploying
BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Development Mode (Optional)
# -----------------------------------------------------------------------------
# Set to true to bypass OAuth and use test user (testing@testing.local)
# WARNING: Only use in development, never in production!
# DEV_MODE=false
